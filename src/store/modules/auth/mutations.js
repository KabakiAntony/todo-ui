export const SET_AUTH_TOKEN = (state,payload) => {
    state.auth_token = payload
 }

 export const SET_IS_LOGGED_IN = (state,payload) => {
   state.isLoggedIn = payload
 }

 export const SET_SCREEN_NAME = (state, payload) => {
   state.screen_name = payload
 }